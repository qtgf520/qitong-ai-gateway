# 綦桐AI网关 完整更新日志

> 包名：com.qtwl.gateway
> 签名证书：qitong.jks (别名: qitong)
> 最后更新：2026-07-15

---

## 🔄 v3.9.3（正式发布）

### 🐛 修复
- **群聊开关自动回弹** — 移除 `isEnabled()` 中的自动关闭逻辑，开关状态完全由用户控制，不再因无参与者自动回弹
- **后台关屏掉线** — 新增 `WakeLock`（PARTIAL_WAKE_LOCK，30分钟上限） + `AlarmManager` 每5分钟定时唤醒，通知栏「唤醒保活」按钮同时控制两种保活机制

### ✨ 新功能
- **启动权限检查** — 打开APP时自动检测：通知权限（Android 13+）、电池优化白名单、唤醒保活开关，缺失时弹窗引导「去设置」一键跳转

### 📋 其他
- 管理页「自启管理」→ 系统权限白名单，通知栏「唤醒保活」→ CPU+Alarm双保活，两者互补

## 🔄 v3.9.2（正式发布）

### 🐛 核心修复

#### 🧠 大脑流式修复
- **大脑 stream=true 返回空** — 修复大脑路径 stream=true 时 SSE 流解析失败导致返回空内容的问题
- **SSE 流解析** — 大脑路径新增 `data: {}` 逐行解析，正确提取 `delta.content`
- **usage 统计** — stream 模式下从 SSE chunk 中提取 usage 数据

#### 🔄 qtai-sj 透传优化
- **参数修正** — qtai-sj 透传路径增加 `sanitizeRequestBody()` 参数校验
- **model 替换** — 改用正则只替换第一个 model 字段，避免全局替换损坏 JSON
- **persona 注入** — 透传路径支持可选人格注入，与正常模型路径对齐

#### 📋 其他
- 备份文件清理（`.bak` 文件）

---

## 🔄 v3.9.1（正式发布）

> 开发：Adybag14-赛博（GitHub PR #2）

### 🌐 多语言修复

#### 🔧 修复（由 Adybag14-赛博 开发）
- **英文回退机制修复** — 切换到英文或其他非中文语言后，多个页面、通知、状态消息和内置AI提示仍显示中文的问题已修复
- **UI页面本地化** — 聊天、数据管理、首页、抓包、设置、统计等页面补充英文显示
- **通知与状态跟随语言** — 服务通知、群聊、思考引导、运行时状态和内置默认名称会跟随当前语言
- **非中文语言回退改进** — 缺少专用翻译时回退到英文，不再回退到简体中文
- **用户内容保护** — 保留用户自定义名称和内容，不会误翻译
- **minSdk 24 兼容** — 用 `Calendar` 替代 API 26 的 `LocalDateTime`，兼容 minSdk 24

#### 🧪 测试覆盖
- 新增单元测试：语言回退、双向状态翻译和自定义内容保护
- Android API 35 模拟器验证：英文→简体中文→English 切换通过
- 最终英文界面无可见中文文本节点

#### 📋 其他
- Lint 报告清理：移除 `LocalDateTime` API 26 警告
- 已知：通知/前台服务代码中仍有 5 个 `NewApi` 警告（非本次改动引入）

---

## 🔄 v3.9.0（正式发布）

### 🐛 核心修复

#### 通知栏流量统计
- **`resetNotificationTraffic()` 位置修复** — 从 proxyRequest 中的上传计数后调用改为最开头（所有计数之前），避免刚记的上传被清零
  - v3.9.0-1 修复：当前会话上传永远为 0 的问题
- **流式路径 `totalDownloadBytes` 缺失** — `pipeStreamResponse` 流式路径只更新通知栏下载，忘了同时更新总下载统计
  - v3.9.0-1 修复：总下载统计不增长

#### 模型使用统计
- **透传路径缺 `recordModelUsage()`** — qtai-sj无前缀透传路径转发后没有调用 `recordModelUsage(finalTarget.modelId)`，统计页"按模型统计(Top 10)"漏记
  - v3.9.0-2 修复：转发真实模型不再漏统计

#### 大脑直连
- **自建 OkHttpClient 不走代理** — 大脑(qtai-sj)路径自建了全新的 OkHttpClient（无代理配置），完全绕过 UpstreamClient 代理系统
  - v3.8.9-4 修复：改用 `UpstreamClient.getDirectClient()` 直连

#### 多语言
- **TranslationManager 新增 ~50 个翻译键** — 底部导航标题、首页网关状态/端口/局域网/服务商管理（添加/编辑/搜索）/模型别名编辑等文字全部可翻译
- **测试覆盖** — 已接入底部导航、顶部标题、首页卡片等位置
### 🧠 技能编码参数传递 + 前缀无空格匹配 + 兜底fallback修复

#### 🚀 新增
- **技能编码支持参数传递** — `【指令:200001:1】`格式，大脑可直接传模型编号/名称
- **200001/200002/100001支持参数执行** — 按编号切模型、按名称模糊匹配、单次测速
- **兜底fallback机制** — 大脑未输出编码时走旧式硬指令匹配，不遗漏任何指令

#### 🔧 修复
- **前缀无空格匹配修复** — `"綦小桐你多大了"`不带空格也能触发大脑
- **群聊空参与者自动关闭** — 防止阻塞大脑路径
- **技能执行结果直接拼入回复** — 大脑输出编码后系统执行并返回结果
- **WakeLock保活** — 添加`PARTIAL_WAKE_LOCK`防止后台掉线

#### 🧪 测试版迭代
- v3.8.9-1（code 115）— WakeLock保活 + 群聊修复 + 技能结果拼入
- v3.8.9-2（code 116）— 参数传递 + 兜底fallback
- v3.8.9-3（code 117）— 前缀无空格匹配修复

---

## 🔄 v3.8.8（正式发布）
### 🧠 人格系统注入大脑 + 当前模型同步

#### 🔧 修复
- **人格信息注入大脑Prompt** — 管理页设定的人格信息（名字/年龄/性格/背景）现在会同步给大脑，问"几岁了"大脑能答
- **当前活动模型同步大脑** — 大脑知道当前活动的真实模型名称，问"当前啥模型"能正确回答

#### 📋 其他
- 代码结构完整，花括号平衡 427/427
- 严格按DEV_GUIDE.md对照备份法开发

---

## 🔄 v3.8.7（正式发布 / 最终版）
### 🧠 綦小桐大脑调度系统 — 技能编码版
#### 🌟 核心架构重构
- **大脑调度取代关键词匹配** — 喊綦小桐后直接进大脑理解，大脑从技能池选最合适的技能执行，不再靠"关键词=动作"死匹配
- **52个技能全覆盖** — 测速/模型切换/网关控制/代理/数据/查询/导航/服务商管理/高级功能，整个APP全可控
- **技能编码系统** — 每个技能有唯一6位编码（如600002=查排行），对外完全不可见，大脑内部分配

#### 💡 智能调度逻辑
- 用户自然语言表达 → 大脑理解语义 → 大脑选择编码技能 → 系统执行 → 大脑组织回复
- 同一件事多种说法都能命中（"排行""哪个最快""排名"→600002）
- 新增功能只需往技能池加编码，不用改匹配规则

#### 🔧 最终修复
- **大脑检测后跳过硬匹配** — 有脑子时走if/else分支直接进大脑，不再执行旧式`parseCommand`
- **大脑Prompt注入技能表** — 大脑知道全部52个技能编码，回复时附带【指令:编码】
- **指令执行改用技能编码** — `extractSkillCodes`+`executeByCode`取代`parseCommand`+`execute`

#### 📊 通知栏流量统计升级
- **双轨制显示** — 通知栏同时显示"当前会话流量（可重置）"和"总统计（持久化）"
- **会话超时自动清零** — 所有会话过期时自动清空通知栏流量

#### 💾 全模型统计→数据库
- TokenUsage表新增 `upload_bytes`/`download_bytes` 字段
- 每次请求记录上传/下载字节到Room数据库
- 数据库迁移6→7自动加列，不丢数据

#### 📋 其他改进
- 通知栏和统计页同步显示总流量
- 旧式硬指令匹配保留（无脑子模型时兼容）
- 调试日志增强（技能执行记录）

---

## 🔄 v3.8.6（正式发布）
### 🐛 修复
- **通知栏切换模型不显示** — 透传路径增加 `activeNodeName` 同步和 `resetNotificationTraffic()` 调用，确保通知栏正确显示当前模型名
- **qtai-sj无前缀透传无回应** — 透明透传路径增加 `finalTarget` 三级兜底（排行榜→activeNodeName→任意已启用模型），全部模型不可用时返回503 JSON错误而非静默失败

### ✨ 新功能
- **会话延迟断开（自适应）** — 每个API会话按闲置超时自动清理，超时时间根据最近5次请求平均延迟自适应调节（基数30秒+延迟×2/1000，范围10-120秒），清理协程每10秒扫描过期会话

### 🛠️ 技术改进
- 新增 `sessionLastActive`/`sessionLatencyHistory` 双Map追踪会话活跃时间和历史延迟
- 新增 `getAdaptiveTimeout()` 自适应超时计算函数
- 新增 `cleanupExpiredSessions()` 定期清理协程
- 所有模型不可用时返回503 JSON错误而非静默丢弃

---

## 🔄 v3.8.5（正式发布）
### 🐛 修复
- **群聊选模型弹窗列表跳动** — 弹窗打开时使用 `remember` 快照数据（`viewModel.enabledModels.value` + `pipelineSortedModelIds.toList()`），避免测速排行榜实时刷新导致勾选位置不断跳动

---

## 🔄 v3.8.4（正式发布）
### ✨ 新功能
- **通知栏流量统计策略优化** — 软件重启或切换模型时，通知栏流量清零重新累计（按会话/按模型）；APP内存储统计保持不变（持久化不重置）
- **群聊模式改进** — 选模型方式从手动输入模型ID改为读取测速排行榜弹窗勾选添加，参与模型+总结模型均支持排行榜排序显示
- **清理旧APK文件** — `/sdcard/Download/` 中的38个旧版本APK已清理，仅保留最新版

### 🛠️ 技术改进
- 新增 `totalUploadBytes`/`totalDownloadBytes` 持久化总统计字段，与 `trafficUploadBytes`/`trafficDownloadBytes`（通知栏可重置）分离
- 新增 `resetNotificationTraffic()` 方法，切换模型时自动重置通知栏流量
- 流量统计双轨制：上传/下载均同时计入通知栏 + 总统计

## 🔄 v3.8.0（正式发布）
### 🚀 多模态+思考引导+群聊模式+全模型统计+短ID恢复
- 🆕 **多模态支持（图片→自动切视觉模型）** — 检测请求含 image_url 时，自动切换至支持视觉的模型
- 🆕 **思考引导可配置** — 管理页新增「🧠 思考引导」卡片，开关+深度选择（关闭/轻度/深度），脑子回复前自动注入思考 prompt
- 🆕 **群聊模式（虚拟沙箱+AI互聊+总结）** — 用户选择N个模型+1个总结者，发消息→AI依次发言→总结者输出结论，AI之间用 [@名称] 互相引用
- 🆕 **全模型流量统计统一入口** — `proxyRequest` 开头统一计 `trafficUploadBytes`，去重6处冗余计费
- 🆕 **流量统计持久化** — 每10秒自动保存到 SharedPreferences，重启不再清零
- 🆕 **统计页显示当前活跃模型** — 网关流量卡片新增 🧠 当前模型行
- 🔧 **恢复短ID匹配** — qtai-sj 透传路径支持完整前缀和短ID两种格式
- 🔧 **刀6修复** — 脑子逻辑加 `actualCmd.isNotBlank()` 保护，无前缀不走脑子
- 🔧 **刀4+刀8修复** — 脑子 catch 返回502错误，不再静默空转
- 🔧 **刀9修复** — brainBody 的 `stream` 参数透传，支持流式
### 🚀 qtai-sj无前缀转发修复 + 通知栏模型兼容
- 🆕 **修复qtai-sj无前缀转发模型错误** — 有前缀脑子回复后不再覆盖 activeNodeName，确保无前缀转发走用户选中的真实模型（如deepseek-v4-flash）
- 🆕 **统一ViewModel转发逻辑** — GatewayViewModel中qtai-sj路径均改用 activeNodeName 替代虚拟模型ID，避免查库失败
- 🆕 **lastRealModel备份机制** — 用户切换qtai-sj时自动保存上一个真实模型，防止丢失

## 🔄 v3.7.6（正式发布）
### 🚀 qtai-sj无前缀转发修复 + 超时优化
- 🆕 **修复qtai-sj无前缀不回复** — 选qtai-sj不喊綦小桐时，用 `activeNodeName`（已切换的真实模型）转发上游
- 🆕 **升级MAX_RETRIES 3→5** — 故障转移重试加多，减少500错误
- 🆕 **脑子超时30s/60s** — 大模型推理慢时不会卡死了
- 🔧 **脑子回复后强制return** — 避免脑子回复后继续走透传导致异常
- 🔧 `forced == "qtai-sj"` 时不再查数据库，改用 `activeNodeName` 正确找真实模型

## 🔄 v3.7.5（已废弃）
### 🚀 修复总输入始终为0 + 补全统计覆盖
- 🆕 **硬指令路径补总输入** — `綦小桐 切换xxx` 等工具指令现在正确累加 `trafficUploadBytes`
- 🆕 **硬指令路径补模型名** — 工具指令执行后通知栏显示 `activeNodeName`
- 🔧 **全路径统计审计** — 确认5条路径全部覆盖：硬指令/脑子/首选模型/故障转移/非chat

## 🔄 v3.7.3（正式发布）
### 🔥 qtai-sj统计修复 + 通知栏实时更新
- 🆕 **通知栏同步模型切换** — 【指令:xxx】切换模型时，`activeNodeName` 立即更新，通知栏实时显示当前模型
- 🆕 **qtai-sj Token统计补全** — 脑子模型返回的 `usage` 正确累加 `tokenPromptInput`/`tokenCompletionOutput`
- 🆕 **qtai-sj记忆绑定** — 脑子回复内容同步写入 `BrainMemoryManager`（用户/AI 对话记忆）
- 🔧 修复 v3.6.8 引入的问题：qtai-sj 路径跳过了数据统计和记忆写入

## 🔄 v3.7.2（正式发布）
### 🛡️ 代码审查全面修复 + API密钥验证 + 访问日志
- 🆕 **API密钥验证** — `validateApiKey()` 拦截器，可在管理页开关，白名单列表管理
- 🆕 **访问日志系统** — 环形缓冲区 1000 条，记录 IP/方法/路径/模型/状态/耗时
- 🆕 **`/v1/logs` 端点** — 查看实时访问日志
- 🆕 **健康检查全面完善** — 返回 version/uptime/模型数/日志数/API密钥状态 等详情
- 🎯 **模型切换优先级修复** — 精确匹配→数字→关键词模糊搜索排行榜，不再乱跳到简写
- 📝 **多模态文本提取统一** — `extractTextContent()` 统一处理字符串/数组格式
- 💰 **Token统计透传** — 脑子模型返回真实usage，工具指令估算prompt_tokens
- 🔑 **会话key精准** — 用完整key的hashCode，不再前20位冲突
- 🖼️ **qtai-sj图片请求自动路由** — 检测图片请求自动选🔁视觉模型
- 🚫 **无可用模型时返回503+JSON** — 不再静默500
- 🔧 全局异常处理统一返回JSON错误体

## 🔄 v3.7.1（正式发布）
### 🐛 豆包调试报告6项修复
- 🆕 **SSE格式标准化** — 所有chunk统一加 `data:` 前缀，首个chunk不遗漏
- 🆕 **500错误统一JSON响应体** — `openAIError()` 返回标准JSON格式 `{"error":{"message":"..."}}`
- 🆕 **无关键词500错误兜底** — 故障转移 + 503明确错误体
- 🆕 **多模态消息格式修复** — content为数组时正确解析提取文本
- 🆕 **Token统计正确透传** — 流式/非流式均透传 `completion_tokens`
- 🆕 **健康检查增强** — 返回 version/port/models_count/running 等详细状态
- 🔧 模型切换持久化修复

## 🔄 v3.7.0（正式发布）
### 🧑 人格名称全程脑子处理，自由对话智能体
- 🆕 **自由思考+回复** — 喊"綦小桐"后脑子自己回复，不透传
- 🆕 **自然语言理解** — 支持"你有几块糖"等日常对话
- 🆕 **指令与聊天一体化** — 脑子可同时处理指令（`【指令:xxx】`）和自由聊天
- 🆕 **智能判断** — 脑子自己决定是否需要转发上游模型
- 🔧 temperature 0.01→0.7，max_tokens 200→500
- 🔧 不再输出JSON指令格式，改为自然语言+指令标记

## 🔄 v3.6.9（正式发布）
### 🏗️ 进程树重构——拆分GatewayScheduler调度层
- 🆕 **GatewayScheduler 单例** — 从 GatewayService 拆出独立调度层
- 🆕 **超预索引** — 启动时全量预排序缓存
- 🆕 **staging原子操作** — snapshot→commit→rollback
- 🆕 **降级日志** — 9种原因不静默
- 🆕 **并行热槽组** — async+awaitAll 真并发热探测
- 🔧 路由层不再直接管理模型调度

## 🔄 v3.6.8（正式发布）
### ⚡ qtai-sj透传转发修复
- 🆕 **脑子说chat后直接透传** — 不走复杂路由（模型排序/记忆/人格）
- 🔧 消除卡顿和断连问题
- 🔧 qtai-sj流式输出优化

## 🔄 v3.6.7（正式发布）
### ⚡ SSE流式卡顿修复 + 指定模型切换
- 🆕 **SSE流式一次性输出** — 取消逐字 delay(20)，消除卡顿
- 🆕 **指定模型切换修复** — regex升级支持 `/` 字符
- 🔧 故障转移 MAX_RETRIES 5→3
- 🔧 脑子 max_tokens 200→300
- 🔧 删除文字扫描误设强制模型逻辑

## 🔄 v3.6.6（正式发布）
### 🧠 全智能思考 + 模型能力标记
- 🆕 **模型能力标记注入脑子** — 排行榜显示 🎨图片/🛠️工具/👁️视觉/💬聊天 标记
- 🆕 **根据能力标记推荐** — 脑子只推荐有对应能力的模型（如图片需求→只选🎨）
- 🆕 **文字兜底提取** — 脑子输出自然语言时，自动扫描文字中模型ID/名称并切换
- 🆕 **非chat路径修复** — `/v1/images/generations` 根据model字段找正确服务商
- 🔧 brainPrompt升级为JSON模式+示例，max_tokens提升到200，temperature=0.01
- 🔧 脑子返回文字时自动匹配排行榜模型并强制切换

---

## 🔄 v3.6.5（正式发布）
### 🧠 全智能思考系统
- 🆕 **脑子带排行榜分析** — brainPrompt注入完整排行榜数据
- 🆕 **智能推荐模型** — 脑子根据模型名称分析能力（图片/视频/代码等），自动推荐并切换
- 🆕 **forceModel脑子解析** — 脑子返回的JSON中提取params.modelId自动切换
- 🆕 **全自动判断** — 用户说"找能生成图片的模型"，脑子自己分析排行榜并切换

### 🔧 优化
- 🔧 brainPrompt 升级为完整智能助手，不再仅限于指令判断
- 🔧 排行榜数据实时注入脑子模型，让脑子知道全部模型信息

---

## 🔄 v3.6.4
### 🧠 大脑记忆注入网关
- 🆕 **buildPersonaPrompt输出记忆完整内容** — 不只是标题
- 🆕 **getSubconscious补充短期记忆** — 不足5条时自动补最近记忆
- 🆕 **网关转发注入记忆+人格** — 上游模型知道你在聊什么

---

## 🔄 v3.6.3
### 🔢 排行编号+当前模型显示
- 🆕 **排行带编号** — 綦小桐 排行 返回编号列表
- 🆕 **编号切换** — 回复数字直接切到对应模型
- 🆕 **查询当前模型** — 綦小桐 当前模型
- 🆕 **首页显示AI模型** — 强制选择上方显示当前模型
- 🔔 **通知栏联动** — 切换模型时日志显示

---

## 🔄 v3.6.2
### ✅ 指令系统调试通过
- 🆕 全面测试通过：硬指令、前缀、脑子模型全部正常

---

## 🔄 v3.6.1
### 🧑 人格名称动态绑定
- 🆕 管理页人格名称自动同步到前缀系统
- 🆕 Toast提示替代Snackbar
- 🆕 切换指定模型ID修复

---

## 🔄 v3.6.0
### 🔥 前缀指令系统
- 🆕 必须加綦小桐/qtai-sj/XiaoTong/自定义人格名才响应
- 🆕 不加前缀直接走正常路由
### 🧠 新增：记忆大脑系统
- 🆕 **BrainMemoryManager** — 独立记忆系统，SharedPreferences存储，不升级数据库（v5不变）
- 🆕 **三级记忆架构** — 短期记忆 → 自动升级为长期记忆 → 潜意识影响行为
- 🆕 **情感感知** — 自动检测 happy/sad/angry/surprised/neutral 情感标签
- 🆕 **重要性评分** — 自动计算0-10分，关键词+长度加权
- 🆕 **保存模式** — 频繁/正常/偶尔三种模式控制记忆密度
- 🆕 **记忆管理UI** — 管理页完整卡片：展开列表、搜索、编辑、删除、情感选择、重要性设置

### 🌐 语言切换修复
- 🔧 **AppCompatDelegate** — 改用 `setApplicationLocales()` 替代手动 `updateConfiguration`
- 🔧 **MainActivity** — 继承 `AppCompatActivity`，切换语言后系统自动 recreate
- 🔧 **appcompat:1.7.0** — 新增依赖

### 🔌 模型能力标记
- 🆕 **ModelCapabilityManager** — SharedPreferences 存储模型能力（supportsTools/supportsVision/supportsImageGen）
- 🆕 **probeModel()** — 测速结束后自动发探针检测模型能力
- 🆕 **自动切换** — 发送消息时能力不匹配自动切换到排行榜下一个支持该能力的模型

### 🔧 修复
- 🐛 **语言切换UI不刷新** — StateFlow响应式 + AppCompatDelegate 系统级切换
- 🐛 **备份EPERM权限** — Android 10+ 改用 MediaStore 方式写入
- 🐛 **Factory冲突** — `Icons.Default.Factory` 和 `GatewayViewModel.Factory` 重名修复

### 📦 其他
- 数据库保持 v5 未升级
- 全部新功能通过 SharedPreferences / 内存实现

---

## 🔄 v3.4.1
### ✨ 新增
- 🆕 **手动强制切换模型** — 排行榜上点击任意模型，立即强制切换到该模型
- 🆕 **📡 实时会话跑马灯** — 首页显示网关实时调用流，带时间戳，从右向左滚动
- 🆕 **歌词式会话流** — 请求一来显示「📤 发送」，回复了显示「📥 回复」，实时更新
- 🆕 **用户消息提取** — 跑马灯显示用户发送的原文（不是JSON），普通人也能看懂
- 🆕 **官方QQ群** — 用户交流群：`1007488535`

### 🔧 修复
- 🐛 **模型页测速空白回复** — 解析返回内容确认有回复，空白=失败
- 🐛 **首页自动测速同样问题** — 排行榜正确显示✅/❌
- 🐛 **跑马灯UI缺失** — 恢复滚动动画，每条错落有致

### 🔄 重构
- 🔄 **排行榜可点击** — 点排行项强制切换，再点取消
- 🔄 **实时会话全链路** — 请求→发送→回复，完整生命周期显示

---

## 🔄 v3.4.0
### ✨ 新增
- 🆕 **聊天室输入框上方思考小字** — AI回复时显示"💭 AI 正在思考..."
- 🆕 **消息操作菜单** — 点击消息气泡弹出操作按钮
- 📋 **消息长按复制** — 长按任意消息自动复制到剪贴板
- ✏️ **用户消息编辑重发** — 编辑后保存并自动重新发送
- 🔄 **用户消息重发** — 一键将内容填入输入框重新发送
- 📤 **系统分享** — 通过系统分享菜单分享消息内容
- 🔄 **AI消息重生成** — 删除最后一条AI消息并自动重新发送
- 💡 **消息时间戳显示** — 每条消息底部显示 HH:mm 时间

### 🔧 修复
- 🐛 **自动故障转移初始化联动** — 启动时自动检测故障转移状态并启动测速，不再需要关/开才生效
- 🐛 **聊天室qtai-sj请求失败** — 改为通过本地网关转发，网关自动选最优模型
- 🐛 **AI返回过快卡闪退** — 优化流式消息更新机制，分离编辑模式与消息列表
- 🐛 **空响应/空白内容触发故障转移** — 上游返回空body或空choices时自动切换模型

### 🔄 重构
- 🔄 **qtai-sj → 🔄 自动化切换** — 全项目名称统一替换
- 🔄 **ModelCard 开关绑定** — 自动化切换模型开关直接绑定独立状态
- 🔄 **故障转移与测速联动** — 开启故障转移自动启动测速，关闭自动停止
---
## 🔄 v3.3.6
### 🐛 Bug修复
- 🐛 `markModelSuccess`传真实延迟而非0（正确记录最快模型）
- 🔧 usage正则`[^}]+`→`[^{}]+`避免嵌套花括号截断
- ⚡ traffic统计`var`→`AtomicLong`（多线程并发不丢数）
- 💰 故障转移探活`max_tokens=3`→`1`（节省API配额）
- 🔳 通知栏点击不新开窗口（singleTask）
- 📊 流水线排行榜Activity重建不丢失（包级全局变量）
### ✨ 新增
- ⚡ **綦桐AI测速虚拟模型（qtai-sj）** — 模型页/聊天/网关三端同步显示
- 🏆 **模型页新增「⚡ 最佳模型同步」分组** — qtai-sj独立显示
- 🌐 **网关`/v1/models`返回qtai-sj** — 第三方APP也可选qtai-sj
- 🎯 **qtai-sj独立于autoFailover** — 不管开关状态，永远走排行榜最快模型
- 🤝 **用户模型永远优先**（失败后才按排行切换）
- 📊 **排行榜始终显示**（不管测速中还是停止）
- 💾 **代理测速走代理**（代理开启时自动走代理）

---
## 🔄 v3.3.5
### ⚡ 优化
- ⚡ 流式输出优化（边读边写，消除卡顿）
- 🧠 会话级模型记忆（同一会话优先用上次成功的模型）
- 🎯 请求模型优先（用户选的模型永远放第一位）
- 🔄 通知栏实时显示模型名（标题+正文）
- 🚀 通知栏更新频率 3s→1s

---

## 🔄 v3.3.1
### 🔧 修复
- ❌ 代理开关异常修复（开关交互、状态同步）
- ❌ 测速逻辑优化（先测海外，不通再测国内）
- ❌ 代理节点导入解析全面精简优化
- ❌ 非代理协议无效分支清理

---

## 🔄 v3.3.0
### 🆕 新增
- **🌐 多语言系统** — 支持15种语言（中文/英文/日文/韩文/法文/德文/西班牙文/俄文/葡文/越南文/泰文/阿拉伯文/印地文/印尼文）
- **🔄 语言自动跟随** — 自动检测手机系统语言，开启后无缝切换
- **👆 手动语言切换** — 管理页新增「🌐 语言设置」卡片，可自由选择15种语言
- **🧠 翻译管理器** — 全局翻译引擎 `TranslationManager` + 快捷函数 `tr(key)`
- **🔧 最优模型记忆 + 4xx/5xx故障转移** — 前序更新整合

### ⚡ 优化
- 底部导航、首页、故障转移、管理页等多处文本已接入多语言

---

## 🔄 v3.2.3
### 🆕 新增
- **🧠 最优模型记忆** — 响应最快的模型自动记住（5分钟缓存），下次请求直接走最优模型
- **🔄 4xx/5xx 触发故障转移** — 上游返回 400/500 等错误时自动切换模型
- **⚡ 流式错误也转移** — 流式请求 4xx/5xx 同样触发故障转移

### 🔧 修复
- ❌ 上游报 `400 temperature invalid` 但不切换模型 → ✅ 自动抛异常切下一个模型
- ❌ 故障转移每次从头试 → ✅ 记住最快的模型，直接优先使用
- ❌ 第三方 APP 参数越界导致重试循环 → ✅ 修正后转发 + 出错直接切

---

## 🔄 v3.2.2
### 🆕 新增
- **🛡️ 请求体参数自动修正** — 自动修正 `temperature`（截取到 [0,2)）、`top_p`、`presence_penalty`、`frequency_penalty`、`max_tokens` 等越界参数
- **🧹 API 兼容性增强** — temperature 设为 2.5 等越界值不再报 400，自动修正为安全值

### 🔧 修复
- ❌ 上游 API 报 `Temperature invalid, should be in [0.0, 2.0)` → ✅ 自动截取到 `1.999`
- ❌ 第三方 APP 发送越界参数导致重试循环 → ✅ 修正后再转发

---

## 🔄 v3.2.1
### 🆕 新增
- **🧠 智能故障转移完全体** — 开启自动切换后，后台并发测速所有启用模型（5秒超时），按响应速度排序
- **⚡ 模型健康缓存** — 测速结果缓存60秒，**最快的模型优先使用**，失败的自动标记不可用
- **🔄 逐级切换** — 一个模型失败→标记为不健康→立即尝试下一个最快模型→直到有模型成功返回
- **🔍 后台自动刷新** — 每个请求触发时检查缓存是否过期，过期自动刷新测速数据

### ⚡ 优化
- **🧵 彻底修复阻塞 IO** — 所有 OkHttp 阻塞调用用 `withContext(Dispatchers.IO)` 切到 IO 线程池
- **🚫 去除 runBlocking** — Ktor 路由 handler 直接调用 suspend 函数，不阻塞 CIO 事件循环
- **📦 流式全量读取** — pipeStreamResponse 一次性读完上游响应再写回，避免 IO 线程阻塞写操作

### 🔧 修复
- ❌ OkHttp execute() 阻塞 Ktor CIO 事件循环 → ✅ withContext(Dispatchers.IO) 异步执行
- ❌ pipeStreamResponse 内 input.read() 阻塞写响应 → ✅ 全量读取后一次写入
- ❌ response.body?.bytes() 在 CIO 线程阻塞 → ✅ 全部在 IO 线程执行

---

## 🔄 v3.2.0
### 🆕 新增
- **🏠 根路径响应** — `GET /` 返回 JSON 状态信息，替代 404 空白，APP 不再报错
- **🔄 故障转移全面修复** — HTTP 请求失败时正确抛出异常，触发自动切换模型
- **⚡ 流式请求前置** — HTTP 请求移到 `respondBytesWriter` 外执行，失败才能触发故障转移
- **🔍 开启时自动检测** — 自动故障转移开启后，按服务商+模型启用状态智能选择候选模型

### 🔧 修复
- ❌ `http://localhost:8889/` 返回空白 404 → ✅ 返回 `{"service":"qitong-ai-gateway","version":"3.2.0","status":"running"}`
- ❌ pipeNormalResponse 异常被吞掉（catch 写错误响应不 re-throw）→ ✅ 抛出异常让 proxyRequest 捕获重试
- ❌ pipeStreamResponse HTTP 请求在 respondBytesWriter 内部执行（失败无法触发故障转移）→ ✅ 移到外部执行
- ❌ 故障转移永远不会触发 → ✅ 现在请求失败自动切换下一个模型，最多试5次

---


## 🔄 v3.0.1
### 🔧 修复
- ❌ 通知栏清零导致网关逻辑中断 — 流量满9秒无变化就归零，大模型思考中被打断
- ✅ **动态累计模式** — 像手机流量一样只增不减，永不归零！加 🟢传输中/⚪空闲 指示灯

---

## 🔄 v3.0.0
### 🆕 新增
- **内置网关抓包工具** — 管理页「网关抓包」卡片，实时记录请求/响应日志（含输入输出流量）
- **备份全面覆盖** — 新增 `代理列表` + `网关端口` 的备份与恢复

### 🔧 修复
- ❌ 网关长流式断开（`callTimeout(600s)` 限制）→ ✅ `callTimeout(0)` + `readTimeout(0)` 无上限
- ❌ OkHttp 无连接池 → ✅ `ConnectionPool(5, 30s)` 连接复用加速
- ❌ 抓包清空按钮 → ✅ 真清空（`clearDebugLogs()` 调用 clear）
- ❌ 抓包按钮颜色不变 → ✅ `debugMode` 改为 StateFlow，Compose 响应式变化

---

## 🔄 v2.6.1
### ⚡ 性能优化
- **网关流式转发提速** — 取消每块 `decodeToString`，改用 `ByteArrayOutputStream` 累积原始字节，流结束后只解码一次
- **去冗余 `runBlocking`** — 流式 usage 解析直接调用 suspend DAO，不再额外包一层

### 🗂️ 备份页面重构
- **合并为一个卡片** — 删除重复的「文件备份」「导入备份」独立卡片，整合到「备份 & 恢复」中
- **定时自动备份** — 新增 ⏰ 开关 + 时间设置（小时/分钟），可设置每天指定时间自动备份到 Downloads
- **布局精简** — 一键恢复、立即备份、定时开关都在一个卡片内完成

---

## 🔄 v2.6.0
### 🆕 新增
- **代理服务商单选互斥** — 开启一个代理时，其他代理自动禁用
- **按模型粒度控制代理** — 模型卡片新增 🔄/🔗 按钮，可单独设置每个模型是否走代理
- **UpstreamClient 直连客户端** — 新增 `getDirectClient()` / `getClientForModel(useProxy)`，不走代理的模型请求直连

### 🔧 修复
- ❌ 代理开关未互斥（开启一个后另一个还是开启状态）
- ✅ 开启一个代理时，自动关闭其他所有代理并持久化

### 🗄️ 数据库
- AiModel 新增 `use_proxy` 字段（默认 true）
- 数据库迁移 v4→v5：添加 `use_proxy` 列

---

## 🔄 v2.5.1
### 🆕 新增
- **模型别名输出修复** — 网关`/v1/models`保持`id`为原始`modelId`不变（**防止第三方客户端调API失败**），`display_name`展示别名，`custom_alias`字段单独返回

### 🔧 修复
- ❌ v2.5.0 误将`id`字段改为别名 → 第三方客户端用`id`调接口找不到模型
- ✅ 恢复`id`保留原始modelId，别名仅影响`display_name`和`custom_alias`展示

---

## 🔄 v2.5.0
### 🆕 新增
- **智能测速** — 同时并发测试百度和谷歌：谷歌通=🌍海外，仅百度通=🇨🇳国内，都不通=❌
- **HTTP/HTTPS代理修复** — 改用`java.net.Proxy`完整限定名解决与OkHttp冲突；HTTPS代理添加自签名SSL信任
- **自动备份 & 一键恢复** — 管理页新增卡片：点击「立即备份」导出到Downloads + 点击「一键恢复」扫描备份文件列表点选恢复
- **服务商禁用联动模型** — `getEnabledModels`改为`INNER JOIN providers p`，禁用服务商后模型自动隐藏

### 🔧 修复
- 测速只猜内外不准确（改为国内外同时测）
- 代理列表不能正确应用HTTP/HTTPS代理
- 服务商禁用但模型仍在列表显示

---

## 🔄 v2.4.1
### 🔧 修复
- 内容中疯狂出现 "null" 文字 — 流式响应`jsonPrimitive?.content`对JSON `null`值已过滤
- 聊天UI适配 — MessageBubble双层防御
- 更新日志保存到工作区 CHANGELOG.md

---

## 🔄 v2.4.0
### 🆕 新增
- 网关Token用量记录（非流式+流式自动解析 usage）
- API连接动态提示（输入地址实时显示最终URL）
- 模型别名输出（网关 `/v1/models` 返回 display_name + custom_alias）
### 🔧 修复
- 统计页面没有数据（网关代理未记录TokenUsage）
- 编辑服务商重复supportingText

---

## 🔄 v2.3.0
### 🆕 新增
- 代理订阅导入（一键订阅、Base64解码、批量去重）
- 剪贴板检测导入
- 手动添加VMESS/SS/VLESS/Trojan/Hysteria2
- 统计页网关流量
### 🔧 修复
- 订阅弹窗点击无响应
- 剪贴板导入闪退（String.startsWith冲突）
- 代理列表不可滑动
- 通知栏闲置不归零

---

## 🔄 v2.2.0
### 🆕 新增
- 代理列表可滑动
- 通知栏自动归零
- 代理卡片格式优化

---

## 🔄 v2.1.0
### 🆕 新增
- 手动添加代理支持扩展协议
### 🔧 修复
- 版本号真实更新
- 订阅/剪贴板崩溃

---

## 🔄 v2.0.0
### 🆕 新增
- 通用代理模式（支持/v1/*所有路径）
- 流式输出开关
- 一键订阅导入
- 剪贴板检测导入
- ProxyLinkParser解析器（vmess/ss/vless/trojan/hysteria2/socks5）
- 局域网IP自动获取+复制
### 🔧 修复
- 网关数据憋死（32KB缓冲区+管道直通）
- 所有Media Type支持
- 超时+body限制解除

---

## 🔄 v1.9.0（初始版本）
- 底部导航栏（首页/服务商/模型/聊天/统计/管理/关于）
- 服务商CRUD
- 模型同步+别名编辑
- 内置聊天
- 网关代理（Ktor Server）
- 代理管理（HTTP/HTTPS/SOCKS5）
- Token用量统计（仅内置聊天）
- 数据备份/恢复/重置
